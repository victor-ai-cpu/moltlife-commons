{
  "name": "moltlife-commons",
  "private": true,
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "dev": "npm -w apps/api run dev & npm -w apps/web run dev",
    "build": "npm -w apps/api run build && npm -w apps/web run build",
    "lint": "npm -w apps/web run lint"
  }
}
